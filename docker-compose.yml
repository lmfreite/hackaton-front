services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        API_BASE_URL: ${API_BASE_URL:-https://api-nexo.stampedev.cloud}
    ports:
      - "8090:80"
    restart: unless-stopped
