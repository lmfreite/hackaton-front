services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        API_BASE_URL: ${API_BASE_URL:-https://api-nexo.stampedev.cloud}
    expose:
      - "80"
    restart: unless-stopped
